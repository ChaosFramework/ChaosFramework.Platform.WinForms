<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">

  <!-- Project Metadata -->
  <PropertyGroup>
    <AssemblyName>ChaosFramework.Platform.WinForms</AssemblyName>
    <RootNamespace>ChaosFramework.Platform.WinForms</RootNamespace>
    <ProjectGuid>{EF4A42BB-C10C-4B68-8F61-CAABB10DF12E}</ProjectGuid>
  </PropertyGroup>

  <!-- Properties -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Properties/Common.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Properties/OutputType/Library.targets" />

  <!-- Configurations -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Configurations.targets" />

  <!-- References -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Platform.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Collections.targets" />

  <ItemGroup>
    <PackageReference Include="OpenTK" Version="4.9.4" />
    <PackageReference Include="OpenTK.GLControl" Version="4.0.2" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="source/**/*.cs" />
  </ItemGroup>

</Project>
